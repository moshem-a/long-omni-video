node_modules/
.env
# Anchored to repo root so they don't also match server/jobs/ (real source).
/uploads/
/jobs/
/out/
*.log
.DS_Store
# Stray Medium article HTML files from other projects (kept out of this repo).
/*-medium-post.html
