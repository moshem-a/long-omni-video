node_modules/
.env
# Anchored to repo root so they don't also match server/jobs/ (real source).
/uploads/
/jobs/
/out/
*.log
.DS_Store
# Belongs to the separate Interview Studio project, not this repo.
/interview-studio-medium-post.html
